export interface NotificationSettings {
  event_added: boolean;
  event_responded: boolean;
  event_commented: boolean;
  event_attendance_changed: boolean;
  chat: boolean;
  groupchat: boolean;
  contact_requested: boolean;
  contact_added: boolean;
  contact_request_approved: boolean;
}
